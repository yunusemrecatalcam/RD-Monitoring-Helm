apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-dashboard-api
spec:
  type: LoadBalancer
  ports:
  - port: 8000
    targetPort: 8000
    name: http
    protocol: TCP
  selector:
    k8s-app: dashboard-api